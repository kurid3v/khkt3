// This file is no longer the entry point for the application.
// The new entry point is managed by the Next.js framework.
// Please run `npm run dev` and navigate to http://localhost:3000 to start the app.

const rootElement = document.getElementById('root');
if (rootElement) {
    rootElement.innerHTML = ''; // Clear the root element
}

console.log("This React application has been migrated to Next.js. Please follow the instructions in index.html to run the development server.");
